{
  "name": "@vibetracker/desktop",
  "private": true,
  "version": "0.1.2",
  "type": "module",
  "description": "The Tauri shell: tray, notifications and a daemon supervisor",
  "scripts": {
    "stage": "node ../../scripts/desktop-stage.mjs",
    "dev": "npm run stage && tauri dev",
    "build": "npm run stage && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
